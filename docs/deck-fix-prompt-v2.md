# Z.ai fix prompt v2 — consolidated (paste as-is)

STYLE LOCK (do not redesign): same dark navy theme, cream serif headlines,
teal eyebrows/accents, coral hero numbers, rounded cards, same 15-slide
order (minus the one deletion below). Change ONLY the listed items.

0. Global: team name is "Hogorithm" (confirmed). Fix "peole" → "people"
   wherever it appears.

1. Problem slide stat cards — replace BOTH cards with these exact numbers
   (do not invent statistics; use these verbatim):
   - Card 1: big "0.2% vs 3.9%", sub "India's recorded PTSD rate vs the
     global lifetime rate — the gap is under-detection, not wellness",
     source "NMHS analysis, Indian J Psychiatry 2023; WHO WMH surveys".
   - Card 2: big "42L+ calls", sub "to Tele MANAS since 2022 — demand is
     proven, yet rural awareness stays low and referrals vanish untracked",
     source "Centre to Lok Sabha, Aug 2026; IMPRI 2026".

2. Solution 02: keep the ANCHOR NOW hero exactly as is. Fix the grounding
   card to "…looping a loved one's recorded voice note, then flows into
   journaling and incident logging." ADD a slim footer strip naming the
   rest of the app so SOS doesn't read as the whole product: "Beyond the
   button: Manage Symptoms · Tools · Get Support — every section shown
   live on the device today."

3. Tech slide: fact-checked, keep all claims. DELETE the "Android-first,
   no iOS" card entirely (do not mention single-platform anywhere);
   redistribute the remaining 7 cards evenly.

4. Scale slide: DELETE the bottom "challenge → mitigation" strip. Fold its
   one honest line into the left column as small text: "Risks, stated
   openly: fragmentation → test matrix; directory rot → quarterly verify;
   no RCT yet." Insert the attached home-screen screenshot into the
   Structural Breakdown (architecture) slide, right side, phone-frame
   crop — do not cover any text.

5. Marketing slide: DELETE the "VA playbook" header and framing — new
   header "Go-to-market". Rewrite realistic and narrow: audience is people
   with diagnosed PTSD symptoms and their families (not "everyone").
   Keep Tele MANAS / NIMHANS / Vandrevala / iCall as alignment targets,
   never as partners. Soften "a triple-tap hardware trigger nobody else
   has" → "a triple-tap trigger no major app offers". Answer all three
   questions on the slide: USP, target market, GTM.

6. DELETE the Unit Economics slide entirely. Keep the SWOT slide.

7. SWOT threats: replace all four with exactly two — "Play Store
   health-claims review (mitigated: we make zero medical claims)" and
   "Support-directory upkeep (mitigated: versioned data, quarterly
   re-verification)". Drop competition, adverse-events, and fragmentation
   rows. A thin threats box is honest, not a gap.

8. Plan slide rewrite — simple, no dataset narrative:
   - 6 MONTHS: "Clinician review — every tool and exercise verified correct
     with experts" + "first institutional pilot prep".
   - 1 YEAR: "Extra languages (native rebuilds, never translation)" +
     "first pilot dataset".
   - BEYOND: "Longitudinal tracking — patterns over months, not moments".
   Keep the learnings strip as is.

Return the full deck, same order minus the deleted economics slide, same
design system. No new statistics beyond those given above.
